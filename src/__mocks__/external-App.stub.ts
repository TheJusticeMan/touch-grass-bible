// Minimal stub of external/App for use in tests.
// Re-exports only what the tested modules depend on, bypassing DOM-heavy and circular imports.
export { Highlighter } from "../external/highlighter";
